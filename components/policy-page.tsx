import Link from "next/link";

export function PolicyPage({ title, intro, sections, updated = "Initial publication draft" }: { title: string; intro: string; sections: { heading: string; paragraphs?: string[]; bullets?: string[] }[]; updated?: string }) {
  return <><section className="page-head container"><span className="eyebrow">Publisher policy</span><h1>{title}</h1><p>{intro}</p><p className="small muted">Status: {updated}</p></section><section className="section"><div className="container policy-layout"><nav className="policy-nav" aria-label="Policy navigation"><Link href="/publication-ethics">Publication Ethics</Link><Link href="/peer-review-policy">Peer Review Policy</Link><Link href="/plagiarism-policy">Plagiarism Policy</Link><Link href="/copyright-policy">Copyright Policy</Link><Link href="/retraction-correction-policy">Retraction &amp; Correction</Link><Link href="/ai-use-policy">AI Use Policy</Link></nav><div className="prose">{sections.map((section) => <section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs?.map((p) => <p key={p}>{p}</p>)}{section.bullets && <ul className="plain-list">{section.bullets.map((b) => <li key={b}>{b}</li>)}</ul>}</section>)}</div></div></section></>;
}
